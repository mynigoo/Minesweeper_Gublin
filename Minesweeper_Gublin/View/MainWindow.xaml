<Window x:Name="MainWindow1"
        x:Class="Minesweeper_Gublin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper_Gublin.ViewModel"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:v="clr-namespace:Minesweeper_Gublin.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" ResizeMode="NoResize" SizeToContent="WidthAndHeight" ScrollViewer.CanContentScroll="True">
    <Window.Resources>
        <local:GameSuperviser x:Key="GameSuperviser" />
    </Window.Resources>
    <Grid x:Name="MainGrid"
           DataContext="{Binding MainMinefield.Cells, Source={StaticResource GameSuperviser}}">
        <ItemsControl x:Name="itemsControl" ItemsSource="{Binding Cells}" Background="DarkGreen" DataContext="{Binding /}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid x:Name="mainUniformGrid" Columns="{Binding NumCols}" Rows="{Binding NumRows}"  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate >
                <DataTemplate>
                    <UserControl Margin="0,0,0,0" Height="25" Width="25" Content="{Binding Title}"  MouseRightButtonUp="Button_MouseRightButtonUp" BorderBrush="White" BorderThickness="0.25" MouseLeftButtonUp="UserControl_MouseLeftButtonUp"  VerticalContentAlignment="Center" HorizontalContentAlignment="Center"   >
                        <UserControl.Style>
                            <Style>
                                <Setter Property="Control.Background"  Value="DarkGreen" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding State}" Value="MARKED">
                                        <Setter Property="Control.Background">
                                            <Setter.Value>
                                                <ImageBrush ImageSource="/Images/no-entry.png"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding State}" Value="OPEN_CLEAR">
                                        <Setter Property="Control.Background" Value="DarkKhaki" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding State}" Value="OPEN_BOMB">
                                        <Setter Property="Control.Background">
                                            <Setter.Value>
                                                <ImageBrush ImageSource="/Images/explosion.png"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </UserControl.Style>
                    </UserControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <v:GameSuperviser x:Name="gameSuperviser">
            <v:GameSuperviser.Style>
                <Style TargetType="{x:Type v:GameSuperviser}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DataContext.Stopp, ElementName=gameSuperviser}" Value="false">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DataContext.Stopp, ElementName=gameSuperviser}" Value="false">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Stopp, Source={StaticResource GameSuperviser}}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </v:GameSuperviser.Style>
        </v:GameSuperviser>
    </Grid>
</Window>
